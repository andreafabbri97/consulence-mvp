# Frontend MVP

Interfaccia Next.js 14 (App Router + Tailwind) per l'MVP. Consuma gli endpoint FastAPI locali.

## Avvio rapido

```bash
npm install
npm run dev
```

Variabili utili:

- `NEXT_PUBLIC_API_BASE` per puntare al backend (default `http://localhost:8000`).
